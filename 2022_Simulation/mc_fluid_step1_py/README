# Phase 1 README：2D 无粘蒙特卡洛流体模拟

## 算法解决的问题

Phase 1 实现了**二维不可压缩无粘欧拉方程的蒙特卡洛求解器**，作为整篇文献复现的基础原型。核心思路是：

- 使用 **涡量-速度（vorticity-velocity）公式**，通过 Biot‑Savart 定律由涡量场重建速度场。
- 采用 **半拉格朗日后向追踪** 平流涡量。
- 利用 **网格缓存** 存储上一时间步的涡量场，避免递归导致的指数复杂度。
- 使用 **均匀蒙特卡洛采样** 估计 Biot‑Savart 积分。

该原型在无边界开放域中模拟了两个高斯涡旋的相互绕转，验证了后向追踪 + MC 估计 + 缓存这一基本流程的可行性。

## 运行代码

1. 进入工程目录：

```bash
cd ./mc_fluid_step1_py
```

2. 运行参数：

```text
--nx                 网格横向分辨率                  默认值: 64
--ny                 网格纵向分辨率                  默认值: 64
--dt                 时间步长                        默认值: 0.1
--nmc                Monte Carlo 采样点数            默认值: 256
--total_time         总模拟时长（秒）                默认值: 10.0
--output_dir         输出目录                        默认值: ./graph
--fps                输出动画帧率                    默认值: 20
```

3.运行示例：

```bash
python main.py --nx 64 --ny 64 --dt 0.1 --nmc 128 --total_time 5
```

## 优点

- **概念简洁**：仅需理解 Biot‑Savart 定律和 Monte Carlo 积分即可实现。
- **无网格生成**：不需要体网格，只需均匀网格存储缓存。
- **点式求解**：可以在任意空间点估计速度/涡量，无需全局求解。
- **易于并行**：每个网格点的计算独立，适合并行化。
- **自动满足不可压缩性**：Biot‑Savart 重建的速度场天然无散。

## 缺点

- **计算效率极低**：每个速度查询都需要在全域均匀采样，样本效率低，导致每步耗时在秒级至分钟级（取决于网格大小和采样数）。
- **数值耗散严重**：网格缓存的双线性插值和半拉格朗日平流会迅速抹平高频涡量细节，最大涡量在 5s 内下降 24%。
- **无边界处理**：无法模拟固体障碍物、流入/流出等实际场景。
- **无方差缩减**：均匀采样的方差大，需要大量样本才能获得可接受的信噪比。
- **仅支持 2D 无粘**：不能处理粘性扩散和三维涡拉伸。

## 局限性

- 当网格分辨率提升或模拟时间增长时，计算成本呈超线性增长，实际不可用。
- 不适用于存在障碍物或边界的场景。
- 初始涡量场的设置需人为给定，缺乏通用性。
- 对蒙特卡洛噪声敏感，模拟结果出现随机尖峰。
