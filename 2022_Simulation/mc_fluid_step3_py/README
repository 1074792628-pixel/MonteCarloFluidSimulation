# Phase 3 README：3D Navier‑Stokes 蒙特卡洛流体模拟

## 算法解决的问题

Phase 3 将模拟从 2D 扩展到 **3D 不可压缩 Navier‑Stokes 方程**，核心贡献是：

- 使用 **Feynman‑Kac 公式** 将粘性和涡拉伸表示为随机过程的期望。
- 算法分为三个子步骤：
  1. **平流**：半拉格朗日后向追踪（同 Phase 1）。
  2. **扩散**：在平流位置上叠加高斯随机扰动，模拟 Wiener 过程。
  3. **涡拉伸**：使用涡量分段法估计应变率张量作用。
- 采用均匀 3D 网格缓存存储涡量（向量场）和速度场，避免递归。
- 输出 z=0 切面的涡量幅值动画。

该阶段首次将 3D NS 方程的蒙特卡洛求解在图形学领域实现，验证了 Feynman‑Kac 公式的实际可行性。

## 运行代码

1. 进入工程目录：

```bash
cd ./mc_fluid_step3_py
```

2. 运行参数：

```text
--nx                 网格 x 方向分辨率               默认值: 16
--ny                 网格 y 方向分辨率               默认值: 16
--nz                 网格 z 方向分辨率               默认值: 16
--dt                 时间步长                        默认值: 0.1
--nmc                Monte Carlo 采样点数            默认值: 8
--nu                 运动粘性系数                    默认值: 0.0
--nd                 扩散子步采样数                  默认值: 4
--total_time         总模拟时长（秒）                默认值: 1.0
--output_dir         输出目录                        默认值: ./output
--fps                输出动画帧率                    默认值: 5
```

3.运行示例：

```bash
python main.py --nx 32 --ny 32 --nz 32 --dt 0.1 --nu 0.0 --nmc 32 --nd 4
```

## 优点

- **真正 3D**：可模拟三维涡旋相互作用（如涡环对撞、交错环）。
- **统一算法框架**：平流/扩散/拉伸三个步骤均为点式估计，与 Phase 1 的理念一致。
- **粘性处理自然**：扩散项通过 Feynman‑Kac 转为随机游走，无需有限差分。
- **扩展性强**：可方便地添加外部力、温度场、浮力等（但未实现）。

## 缺点

- **计算量天文数字**：3D 网格点数随分辨率立方增长，MC 采样数依赖同样大，每步耗时（64³）可达数十秒至数分钟。
- **物理缺失（Phase 3 早期版本）**：在 \(\nu=0\) 时错误地跳过了拉伸项，导致 3D 物理不正确（涡拉伸在无粘中必须存在）。
- **数值耗散严重**：三线性插值和低采样导致总环量下降 15~20%，最大涡量出现异常尖峰。
- **可视化仅切面**：完整 3D 流场难以展示和验证。

## 局限性

- 无边界处理：仅适用于开放域，不能处理固体障碍物。
- 无方差缩减：均匀采样在整个域内效率极低。
- 存储和内存需求大：3D 向量场需要 3× 浮点数，高分辨率下内存占用很大。
- 涡拉伸项使用的涡量分段法（vortex segment method）精度有限，且依赖速度场插值，会引入额外误差。
