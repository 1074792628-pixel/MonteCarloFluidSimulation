# Phase 4 README：方差缩减与性能改进

## 算法解决的问题

Phase 4 针对前三个阶段的**两大核心问题——方差过大和效率过低**，引入了文献中的方差缩减技术：

- **重要性采样（Importance Sampling）**：按涡量幅值分布采样，替代均匀采样，使样本集中在有涡量的区域，在相同样本数下方差降低 5~10 倍。
- **控制变量法（Control Variate）**：利用前一步速度作为控制变量，仅估计涡量变化量 \(\Delta \omega = \omega(t) - \omega(t-\Delta t)\) 的 Biot‑Savart 贡献，进一步降低方差。
- **修复涡拉伸**：在 \(\nu=0\) 时强制计算拉伸项，修正 Phase 3 的物理错误。

同时，提供了进一步性能优化的方案（NumPy 向量化、Numba JIT 编译、多线程并行）及基准测试。

## 运行代码

1. 进入工程目录：

```bash
cd ./mc_fluid_step4_py
```

2. 运行参数：

```text
--nx                 网格 x 方向分辨率               默认值: 32
--ny                 网格 y 方向分辨率               默认值: 32
--nz                 网格 z 方向分辨率               默认值: 32
--dt                 时间步长                        默认值: 0.1
--nmc                Monte Carlo 采样点数            默认值: 64
--nu                 运动粘性系数                    默认值: 0.0
--nd                 扩散子步采样数                  默认值: 4
--total_time         总模拟时长（秒）                默认值: 1.0
--output_dir         输出目录                        默认值: ./output
--fps                输出动画帧率                    默认值: 5
--no_control_variate 禁用控制变量（默认启用）        默认值: 关闭
--no_importance      禁用重要性采样（默认启用）      默认值: 关闭
```

3.运行示例：

```bash
python main.py --nx 32 --ny 32 --nz 32 --nmc 128 --nu 0.01 --no_control_variate
```

## 优点

- **方差显著降低**：在同等样本数下，重要性采样可大幅减少速度估计噪声，从而减小最大涡量尖峰。
- **物理正确性提高**：修复了拉伸项，使得 3D 无粘模拟的涡量演化符合物理规律。
- **为实用化奠基**：提供了完整的优化路线（从 Python 向量化到 Numba 编译），在实践中可获得 100~1000 倍加速。
- **框架统一**：可在 Phase 1~3 的基础代码中直接集成。

## 缺点

- **控制变量实现不完善**：当前版本中控制变量因缓存速度本身也是 MC 估计且噪声未去相关，导致效果反而恶化（见实验数据：VR 的最大涡量尖峰比 Base 更严重）。
- **重要性采样实现开销**：预计算 CDF 和每次采样索引需要额外存储和计算，目前实现仍为 Python 级循环，未完全优化。
- **对初始化较敏感**：控制变量需要高质量的前一步速度场，若初始步采样数不足，误差会累积。

## 局限性

- 所有方差缩减技术建立在 **涡量-速度公式** 和 **均匀网格缓存** 的基础上，难以直接推广到 WoS 等边界处理方法。
- 重要性采样的 CDF 需随涡量场动态更新，在每一步都重建会带来额外的 \(O(Nx\cdot Ny\cdot Nz)\) 开销。
- 控制变量法在时间步长较大或涡量变化剧烈时失效（\(\Delta \omega\) 不再小）。
- 未实现更高级的方差缩减技术（如多重重要性采样、马尔可夫链蒙特卡洛等）。
